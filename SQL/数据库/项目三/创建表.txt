use stud_info_database
go
create table Depart_tab
(
id int not null identity(1,1),
departID varchar(2) not null constraint PK_departID primary key,
departNAME varchar(20) not null
)
create table Nation_tab
(
id int not null identity(1,1),
nationID varchar(2) not null constraint PK_nationID primary key,
nationNAME varchar(12) not null
)
create table Specialty_tab
(
id int not null identity(1,1),
specialtyID varchar(3) not null constraint PK_specialtyID primary key,
specialtyNAME varchar(30) not null
)
create table gender_tab
(
id int not null identity(1,1),
genderID varchar(1) not null constraint PK_genderID primary key,
genderNAME varchar(2) not null 
)
create table title_tab
(
id int not null identity(1,1),
titleID varchar(2) not null constraint PK_titleID primary key,
titleNAME varchar(20) not null
)
create table Teacher_tab
(
id int not null identity(1,1),
teacherID varchar(20) constraint PK_teacherID primary key,
teacherNAME varchar(10) not null,
genderID varchar(1) not null,
nationID varchar(2) not null,
titleID varchar(2) not null,
departID varchar(2) not null
)
create table Course_tab
(
id int not null identity(1,1),
courseID varchar(10) not null constraint PK_courseID primary key,
courseNAME varchar(20) not null,
courseXIZI varchar(12),
teacherID varchar(20) not null,
term varchar(10),
first_course varchar(10),
departID varchar(2),
classHour varchar(3) not null,
credits varchar(3) not null
)
create table Student_tab
(
id int not null identity(1,1),
studentID varchar(15) constraint PK_studentID primary key,
dtudentNAME varchar(10) not null,
genderID varchar(1) not null,
nationID varchar(2) not null,
brithday date,
specialtyID varchar(3) not null,
departID varchar(2) not null,
tel varchar(11),
qq varchar(20),
e_mail varchar(30),
credits varchar(3),
note text
)
create table Score_tab
(
id int not null identity(1,1),
studentID varchar(15) not null constraint PK_ primary key,
courseID varchar(10) not null,
score varchar(3) not null
)
go